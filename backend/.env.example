# Production Environment Variables
# Copy these to your deployment platform (Railway/Render/etc.)

# Server Configuration
PORT=3001
NODE_ENV=production

# Frontend URL (replace with your Vercel URL)
FRONTEND_URL=https://your-vercel-app-url.vercel.app

# JWT Configuration
JWT_SECRET=your-secure-jwt-secret-here

# MongoDB Configuration
MONGODB_URI=your-mongodb-connection-string

# Google OAuth Configuration (from Google Cloud Console)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Important: Never commit this file to git!
# Each deployment platform will set these as environment variables